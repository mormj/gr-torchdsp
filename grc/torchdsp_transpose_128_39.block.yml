id: torchdsp_triton_transpose_128_39
label: Triton Transpose 128x39
category: '[torchdsp]'

templates:
  imports: import torchdsp
  make: torchdsp.triton_block("${"transpose_128_39_cpu_openvino" if device == 'cpu' else "fft_gpu"}", ${max_batch_size}, ${triton_url}, [8*128*39], [8*39*128])

parameters:
- id: triton_url
  label: "Triton URL"
  dtype: string
  default: "localhost:8000"
- id: device
  label: "Device"
  dtype: enum
  options: [cpu, gpu]
  default: 'cpu'
- id: max_batch_size
  label: "Max Batch Size"
  dtype: int
  default: 2

inputs:
- label: in
  domain: stream
  dtype: complex
  vlen: 4992

outputs:
- label: out
  domain: stream
  dtype: complex
  vlen: 4992

file_format: 1
