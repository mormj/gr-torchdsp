id: torchdsp_triton_log10
label: Triton Log10
category: '[torchdsp]'

templates:
  imports: import torchdsp
  make: torchdsp.triton_block("${"log10_cpu_openvino" if device == 'cpu' else "log10_gpu"}", ${max_batch_size}, ${triton_url}, [4*128], [4*128])

parameters:
- id: triton_url
  label: "Triton URL"
  dtype: string
  default: "localhost:8000"
- id: device
  label: "Device"
  dtype: enum
  options: [cpu, gpu]
  default: 'cpu'
- id: max_batch_size
  label: "Max Batch Size"
  dtype: int
  default: 256

inputs:
- label: in
  domain: stream
  dtype: float
  vlen: 128

outputs:
- label: out
  domain: stream
  dtype: float
  vlen: 128

file_format: 1
